# Package Update — 2026-05-12 (minor)

## Updated packages

| Package | From | To | Type |
|---------|------|----|------|
| @tailwindcss/postcss | 4.2.2 | 4.3.0 | patch/minor |
| tailwindcss | 4.2.2 | 4.3.0 | patch/minor |
| tailwind-merge | 3.5.0 | 3.6.0 | minor |
| lucide-react | 1.8.0 | 1.14.0 | minor |
| shadcn | 4.3.0 | 4.7.0 | minor |
| zod | 4.3.6 | 4.4.3 | minor |

## Changelogs

---

### @tailwindcss/postcss & tailwindcss  4.2.2 → 4.3.0

Source: https://github.com/tailwindlabs/tailwindcss/releases

#### v4.3.0
- Container size utilities, scrollbar styling, zoom and tab utilities
- `@variant` support for stacked and compound variants
- Functional `@utility` definitions with `--default()` support
- Canonicalization enhancements for arbitrary values and unit preservation

#### v4.2.4
- Fixed Vite aliases in `@import` and `@plugin` directives with `@tailwindcss/vite`

#### v4.2.3
- Improved tracking utilities, arbitrary value collapsing, border/scroll/overflow combinations
- Faster JSONL file scanning

---

### tailwind-merge  3.5.0 → 3.6.0

Source: https://github.com/dcastil/tailwind-merge/releases

#### v3.6.0
- Support for Tailwind CSS v4.3 with new `postfixLookupClassGroups` option (named container queries with slash in class name)
- Support for readonly array values in configuration

---

### lucide-react  1.8.0 → 1.14.0

Source: https://github.com/lucide-icons/lucide/releases

#### v1.9.0 – v1.14.0
- New icons: `timeline`, `heart-x`, `layers-minus`, `bell-check`, `folder-bookmark`, `waves-vertical`, `repeat-off`
- `.mjs` ESM bundles, Angular string input support, Svelte improvements
- Font codepoint collision detection

---

### shadcn  4.3.0 → 4.7.0

Source: https://github.com/shadcn-ui/ui/releases

#### v4.7.0
- Add support for package imports
- Allow alias placeholders in target for registry items
- Add previous version error suggestion

#### v4.6.0
- Add `shadcn preset` commands
- Add preset info to `npx shadcn info`
- Fix apply in monorepo

#### v4.5.0
- Add `--pointer` option to CLI (`npx shadcn init --pointer` enables cursor: pointer on buttons)

#### v4.4.0
- Add `apply --only` flag
- Fix chartColor in presets

#### v4.3.1
- Ensure `init` only runs template post-init hooks for newly created projects
- Improved request headers (Accept, User-Agent)

---

### zod  4.3.6 → 4.4.3

Source: https://github.com/colinhacks/zod/releases

#### v4.4.0 ⚠ Breaking changes
- Tuple defaults now materialize output values correctly
- Required object properties with `z.undefined()` strictly enforced
- `.merge()` throws when receiver has refinements
- Base64 validation rejects whitespace
- Stricter string validators (CUID, HTTP URL)
- Record key transforms now execute
- Improved tree-shaking with pure function annotations
- New locales: Croatian, Greek, Romanian

#### v4.4.1
- Fix: reject tuple holes before required defaults

#### v4.4.2
- `z.preprocess` defers optionality to inner schema

#### v4.4.3
- Restore catch handling for absent object keys
- Generalize optin/fallback to transform; restore preprocess on absent keys
